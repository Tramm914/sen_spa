# Artifact 7b: Quiz tự kiểm tra trước khi Merge

1. **Câu hỏi:** Điều gì sẽ xảy ra với code cũ nếu mảng `bookings` chỉ có 3 phần tử?
   - [ ] Đếm đúng 3 phần tử.
   - [x] Văng lỗi `Cannot read properties of undefined (reading 'ngay')` ở vòng lặp thứ 4.

2. **Câu hỏi:** Lỗi gõ cứng chuỗi `"2026-07-27"` thuộc nhóm (họ) bug nào?
   - [ ] Biên (Boundary)
   - [x] Giá trị ma thuật (Magic String/Number)
   - [ ] Dữ liệu rỗng (Null/Empty)

3. **Hành động:** Bạn có cam kết sẽ luôn kiểm tra `length` của mảng thay vì fix cứng số khi dùng vòng lặp for không?
   - [x] Có, tôi đã hiểu và sẽ sửa lại dùng `bookings.length`.